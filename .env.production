# API base URL for production builds served from static hosting (e.g. Supreme Center)
# The fetch interceptor in main.jsx will prefix all /api/ calls with this URL.
# Leave empty (or don't set) when running the Node.js server directly (Render, local dev).
VITE_API_BASE=https://digiscribedev2026.onrender.com
