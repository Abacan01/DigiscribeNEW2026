/**
 * Passenger (cPanel Node.js) entry point.
 * cPanel's "Setup Node.js App" expects an app.js in the application root.
 * It imports the Express server which exports the app.
 */
import './server/server.js';
